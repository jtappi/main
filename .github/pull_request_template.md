## Summary
<!-- What does this PR do? One paragraph. -->

---

## Test Coverage

> Consult `docs/testing/README.md` and the relevant project catalog before filling this out.
> Claude must read `docs/testing/` at the start of every PR session.

### Existing tests affected by this PR
<!-- List any existing tests that exercise the code paths changed by this PR.
     If none, write "None." -->

### New tests added in this PR
<!-- For each new test, state: file path, classification (Unit / Integration / Smoke / Critical / Regression), and what it covers.
     Example:
     - `tests/unit/auth.test.js` — Unit — hashPassword returns consistent output for same input
     If no new tests were added, explain why in the section below. -->

### Tests not added and why
<!-- If this PR adds or changes behavior but no new tests were written, explain the reason.
     "No behavior changed" is a valid reason. "Not worth it" is not.
     If tests are deferred, open a tracking issue and link it here. -->

### `docs/testing/` updated?
<!-- If this PR adds new tests, the relevant project catalog in docs/testing/ must be updated
     in the same PR. Confirm it has been updated, or explain why no update is needed. -->
- [ ] `docs/testing/PORTAL.md` updated (or N/A)
- [ ] `docs/testing/TRACKMYWEEK.md` updated (or N/A)
- [ ] `docs/testing/README.md` updated (or N/A — only needed for new projects)

---

## Deploy steps after merge
<!-- List any manual steps required after merging (git pull, pm2 restart, npm run build, etc.).
     If none, write "None." -->
