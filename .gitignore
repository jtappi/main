# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.*

# Data files with personal info
core/data/users.json

# TrackMyWeek runtime data — templates committed, live files gitignored
trackmyweek/data/data.json
trackmyweek/data/questions.json

# macOS
.DS_Store

# Logs — ignore generic log files but keep the structured test run log
logs/*.log
npm-debug.log*
!logs/test-runs.jsonl

# PM2
.pm2/
