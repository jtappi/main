# Portal
PORTAL_PORT=3000

# Session — generate a long random string, never commit the real value
SESSION_SECRET=replace-this-with-a-long-random-string

# BP Tracker — Claude Vision extraction
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=replace-this-with-your-anthropic-api-key
