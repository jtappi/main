import React from 'react';

/**
 * Reports — reports view (Phase 3 stub).
 *
 * Full implementation in Phase 5:
 *   - Summary cards (30-day avg, 30-day median, 7-day avg)
 *   - Trend chart (Chart.js line chart)
 *   - Reading history table with inline notes edit and delete
 */
export default function Reports({ user }) {
  return (
    <div className="reports-view" data-testid="reports-view">
      <h1 className="reports-heading">Reports</h1>
      <p className="reports-stub">Coming in Phase 5.</p>
    </div>
  );
}
