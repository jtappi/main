{
  "name": "prisondonkey",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page. No dependencies — served by the portal's express instance.",
  "scripts": {
    "start": "echo 'prisondonkey has no standalone server. Mount via portal.'"
  }
}
